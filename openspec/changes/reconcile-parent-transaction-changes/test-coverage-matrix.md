# Reconciliation Test Coverage Matrix

This matrix is executable contract data. Scenario names exactly match every normative `#### Scenario:` heading in `specs/parent-transaction-reconciliation/spec.md`. References use the exact `SpecClass: feature name` executed by `testAll`.

| Normative scenario | Focused executable test | Integration test |
|---|---|---|
| Amount edit retains source identity | `SourceRevisionNormalizerSpec: top-level identity excludes mutable fields` | `ReconciliationPlanningIntegrationSpec: stable ordinary split and component identities remain distinct in SQLite lineage` |
| Split component retains source identity | `SourceRevisionNormalizerSpec: split identity excludes mutable fields` | `ReconciliationPlanningIntegrationSpec: stable ordinary split and component identities remain distinct in SQLite lineage` |
| Ordinary and split identities remain distinct | `SourceRevisionNormalizerSpec: ordinary and split identities do not collide` | `ReconciliationPlanningIntegrationSpec: stable ordinary split and component identities remain distinct in SQLite lineage` |
| Deleted transaction tombstone is retained | `SourceRevisionNormalizerSpec: deletion normalizes as tombstone with server knowledge` | `ParentChildBudgetSyncerWireMockSpec: tombstone unapproval and unmapping delete mirrors without reversals` |
| Empty delta advances knowledge | `SyncRunCoordinatorSpec: successful run advances cursor to latest server knowledge` | `ParentChildBudgetSyncerWireMockSpec: parent money movement failure does not block transaction cursor` |
| Older transaction edit is requested through delta | `YnabBudgetRepositorySpec: empty incremental transaction response retains response server knowledge for cursor advancement` | `ParentChildBudgetSyncerWireMockSpec: second live cycle uses saved transaction cursor and durable reconciliation state prevents reposting` |
| Partial split data cannot imply removal | `SourceRevisionNormalizerSpec: partial split marks complete fetch required` | `ReconciliationPlanningIntegrationSpec: split add edit remove touches only matching components and partial fetch cannot delete` |
| Date amount and payee edit updates in place | `ParentTransactionReconcilerSpec: authoritative changes plan one update with recreation payload` | `ParentChildBudgetSyncerWireMockSpec: authoritative edit updates one child preserves memo and stable lineage` |
| Same-budget account reroute updates in place | `ParentTransactionReconcilerSpec: same-budget reroute plans update` | `ReconciliationMutationIntegrationSpec: authoritative update preserves child memo and stable source mirror lineage` |
| Child approval drift is corrected when source is observed | `ParentTransactionReconcilerSpec: observed approval drift plans update` | `ReconciliationMutationIntegrationSpec: authoritative update preserves child memo and stable source mirror lineage` |
| Parent memo-only edit is a child no-op | `ParentTransactionReconcilerSpec: memo-only edit plans no financial mutation and requires verification` | `ReconciliationPlanningIntegrationSpec: memo-only observation persists revision and verifies mirror without financial mutation` |
| Financial edit preserves child memo | `ReconciliationOperationApplierSpec: update is partial preserves memo enforces cleared unapproved and skips an already matching retry` | `ParentChildBudgetSyncerWireMockSpec: authoritative edit updates one child preserves memo and stable lineage` |
| Memo-only edit still verifies mirror existence | `ParentTransactionReconcilerSpec: memo-only edit plans no financial mutation and requires verification` | `ReconciliationPlanningIntegrationSpec: memo-only observation persists revision and verifies mirror without financial mutation` |
| Parent deletion removes mirror | `ParentTransactionReconcilerSpec: tombstone unapproval and mapped-to-unmapped plan deletion` | `ParentChildBudgetSyncerWireMockSpec: tombstone unapproval and unmapping delete mirrors without reversals` |
| Approval withdrawal removes mirror | `ParentTransactionReconcilerSpec: tombstone unapproval and mapped-to-unmapped plan deletion` | `ParentChildBudgetSyncerWireMockSpec: tombstone unapproval and unmapping delete mirrors without reversals` |
| Mapped source becomes unmapped | `ParentTransactionReconcilerSpec: tombstone unapproval and mapped-to-unmapped plan deletion` | `ParentChildBudgetSyncerWireMockSpec: tombstone unapproval and unmapping delete mirrors without reversals` |
| Repeated deletion is idempotent | `ReconciliationOperationApplierSpec: delete treats already absent as success and unlocks dependent create in the same batch` | `ReconciliationMutationIntegrationSpec: deletion unapproval and unmapping intents delete without reversal and absence is idempotent` |
| Ordinary transaction becomes split | `ParentTransactionReconcilerSpec: ordinary-to-split orders delete then creates` | `ReconciliationPlanningIntegrationSpec: ordinary split transitions are complete deterministic and preserve unrelated mirrors` |
| Split transaction becomes ordinary | `ParentTransactionReconcilerSpec: split-to-ordinary orders deletes then create` | `ReconciliationPlanningIntegrationSpec: ordinary split transitions are complete deterministic and preserve unrelated mirrors` |
| Split component is added or edited | `ParentTransactionReconcilerSpec: split add edit and removal touch only matching components` | `ReconciliationPlanningIntegrationSpec: split add edit remove touches only matching components and partial fetch cannot delete` |
| Split component is removed | `ParentTransactionReconcilerSpec: split add edit and removal touch only matching components` | `ReconciliationPlanningIntegrationSpec: split add edit remove touches only matching components and partial fetch cannot delete` |
| Cross-budget reroute replaces mirror | `ParentTransactionReconcilerSpec: cross-budget reroute creates dependency chain` | `ParentChildBudgetSyncerWireMockSpec: cross-budget reroute separates tokens deletes once and retries only replacement create` |
| Configuration changes alone do not rewrite history | `ParentTransactionReconcilerSpec: config-only cycle plans no historical work and later delta uses current mapping` | `ReconciliationPlanningIntegrationSpec: configuration-only cycle plans nothing and a later delta uses current routing` |
| Manually deleted mirror is recreated on parent edit | `ReconciliationOperationApplierSpec: missing update recreates on authoritative or no-op intent and retains child lineage` | `ReconciliationMutationIntegrationSpec: missing child recreation retains prior child ID audit history` |
| Missing mirror is recreated after a no-op source edit | `ParentTransactionReconcilerSpec: memo-only edit plans no financial mutation and requires verification` | `ParentChildBudgetSyncerWireMockSpec: no-op parent edit recreates a missing mirror and advances cursor` |
| Second live process aborts on lock contention | `SyncStateLockSpec: second acquire against the same database aborts with a clear message` | `SyncStateLockIntegrationSpec: closing the holder unblocks a waiting second acquire` |
| Abrupt exit releases the live lock for the next run | `SyncStateLockSpec: release allows a later process to acquire the same lock` | `SyncStateLockIntegrationSpec: lock survives holder process exit simulation and is re-acquirable` |
| Dry-run does not take the live lock | `ParentChildBudgetSyncerSpec: fromConfig dry run does not create a SQLite file or parent directory` | `SyncStateLockIntegrationSpec: dry-run style missing parent directory is created only for the lock file path when acquiring` |
| Update failure blocks cursor | `SyncRunCoordinatorSpec: failed update blocks transaction cursor until ingestion recovery` | `ReconciliationRecoveryIntegrationSpec: three fresh processes recover mixed transaction and movement update delete work independently` |
| Cross-budget create fails after delete | `ReconciliationOperationApplierSpec: delete treats already absent as success and unlocks dependent create in the same batch` | `ParentChildBudgetSyncerWireMockSpec: cross-budget reroute separates tokens deletes once and retries only replacement create` |
| Three process-like cycles recover mutation work | `SyncRunCoordinatorSpec: failed update blocks transaction cursor until ingestion recovery` | `ReconciliationRecoveryIntegrationSpec: three fresh processes recover mixed transaction and movement update delete work independently` |
| Older incomplete transaction batch blocks newer cursor advancement | `SyncRunCoordinatorSpec: cursor advances only when caller reports all transaction batches complete` | `ReconciliationRecoveryIntegrationSpec: older incomplete transaction batch blocks cursor while newer batch is fully applied` |
| Remote mutation succeeds before local completion write fails | `ReconciliationOperationApplierSpec: update and delete crash windows recover from remote state or successful attempts` | `ReconciliationMutationIntegrationSpec: remote create update and delete crash windows recover without a second financial effect` |
| Fresh initialization creates baseline version 1 | `SyncStateStoreIntegrationSpec: fresh initialization creates exactly the baseline tables and version` | `SyncStateStoreIntegrationSpec: fresh initialization creates exactly the baseline tables and version` |
| Repeated initialization is unchanged | `SyncStateStoreIntegrationSpec: initialization is repeatable` | `SyncStateStoreIntegrationSpec: initialization is repeatable` |
| Nonempty unversioned database is rejected unchanged | `SyncStateStoreIntegrationSpec: nonempty unversioned database is rejected without mutation` | `SyncStateStoreIntegrationSpec: nonempty unversioned database is rejected without mutation` |
| Schema version history must be contiguous | `SyncStateStoreIntegrationSpec: schema version gaps are rejected` | `SyncStateStoreIntegrationSpec: schema version gaps are rejected` |
| Newer schema version is rejected unchanged | `SyncStateStoreIntegrationSpec: newer contiguous schema is rejected` | `SyncStateStoreIntegrationSpec: newer contiguous schema is rejected` |
| Baseline failure rolls back atomically | `SyncStateStoreIntegrationSpec: baseline failure rolls all schema work back` | `SyncStateStoreIntegrationSpec: baseline failure rolls all schema work back` |
| Dry-run reports destructive work | `ParentTransactionReconcilerSpec: tombstone unapproval and mapped-to-unmapped plan deletion` | `DryRunReconciliationIntegrationSpec: dry run reports create update delete reroute and recreation with byte-identical SQLite` |
| Dry-run missing database creates nothing | `ReconciliationStateStoreIntegrationSpec: dry-run missing database reads empty and creates nothing` | `ReconciliationStateStoreIntegrationSpec: dry-run missing database reads empty and creates nothing` |
| Dry-run reads supported state without changing bytes | `ReconciliationStateStoreIntegrationSpec: dry-run reads supported state without changing database bytes` | `ReconciliationStateStoreIntegrationSpec: dry-run reads supported state without changing database bytes` |
| Dry-run rejects unsupported state without mutation | `ReconciliationStateStoreIntegrationSpec: dry-run rejects unsupported old database without mutation` | `ReconciliationStateStoreIntegrationSpec: dry-run rejects unsupported old database without mutation` |
| Verified update payload is sent | `YnabBudgetRepositorySpec: child transaction update sends only supplied supported fields in wrapper` | `YnabTransactionMutationWireMockSpec: verified update uses child token documented endpoint and memo-preserving payload` |
| Verified delete endpoint is idempotent | `YnabBudgetRepositorySpec: child transaction delete maps success and treats 404 as already absent` | `YnabTransactionMutationWireMockSpec: verified delete endpoint treats documented absence as idempotent` |
| Same-ID amount or category change retains movement identity | `MoneyMovementNormalizerSpec: movement identity excludes mutable fields` | `MoneyMovementReconciliationIntegrationSpec: same movement ID edits retain persisted lineage and update both independent sides` |
| Inflow and outflow sides cannot collide | `MoneyMovementReconcilerSpec: direction distinguishes same-child sides` | `MoneyMovementReconciliationIntegrationSpec: same movement ID edits retain persisted lineage and update both independent sides` |
| Same-ID movement amount edit updates existing sides | `MoneyMovementReconcilerSpec: amount edit plans side updates` | `MoneyMovementReconciliationIntegrationSpec: same movement ID edits retain persisted lineage and update both independent sides` |
| Same-ID movement category reroute reconciles sides | `MoneyMovementReconcilerSpec: category edit compares complete side set` | `MoneyMovementReconciliationIntegrationSpec: same-ID category reroute uses updates and ordered cross-budget replacement` |
| Old observed movement remains eligible for correction | `MoneyMovementReconcilerSpec: existing mirror bypasses new-movement lookback` | `MoneyMovementReconciliationIntegrationSpec: old observed movement remains correctable beyond lookback and unchanged snapshots are idempotent` |
| Movement disappears from a later snapshot | `MoneyMovementReconcilerSpec: absent ID becomes unconfirmed without delete` | `MoneyMovementReconciliationIntegrationSpec: snapshot absence and replacement-like IDs preserve prior mirrors non-destructively` |
| New movement ID does not prove replacement | `MoneyMovementReconcilerSpec: similar replacement and shared group remain independent` | `MoneyMovementReconciliationIntegrationSpec: snapshot absence and replacement-like IDs preserve prior mirrors non-destructively` |
| Movement mutation failure does not block transaction cursor | `SyncRunCoordinatorSpec: movement failure permits cursor when transaction batch completed` | `ReconciliationRecoveryIntegrationSpec: three fresh processes recover mixed transaction and movement update delete work independently` |
| Repeated complete snapshot does not duplicate movement work | `MoneyMovementReconcilerSpec: unchanged snapshot plans no operation beyond verification` | `MoneyMovementReconciliationIntegrationSpec: old observed movement remains correctable beyond lookback and unchanged snapshots are idempotent` |
| Operator reviews destructive semantics before rollout | `DocumentationContractSpec: operator onboarding links the destructive reconciliation guide` | `ReconciliationDocumentationIntegrationSpec: guide and onboarding present one destructive dry-run workflow` |
| Coverage matrix is complete | `ReconciliationCoverageContractSpec: matrix scenario set exactly equals normative specification` | `ReconciliationCoverageIntegrationSpec: every matrix integration feature is discoverable and selected by testAll` |
| Semantics change during implementation | `ReconciliationCoverageContractSpec: matrix references exact executable focused features` | `ReconciliationCoverageIntegrationSpec: guide matrix and specification retain the reconciliation contract` |
